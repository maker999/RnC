# this piece of code rquests key information from the board through the serial port and displays them.
# please install the following dependencies in order to use the server code:
# $ npm install serialport
# $ npm install express
